# universal-memory-service
Self-hosted universal memory service for AI agents. Hybrid search (vector + BM25 + knowledge graph) with reranking, query expansion, and multi-platform support via HTTP API and MCP.
